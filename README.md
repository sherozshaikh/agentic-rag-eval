# agentic-rag-eval
RAG system with adaptive retrieval (Qdrant dense + sparse + RRF), cross-encoder re-ranking, and long-term memory (Mem0) — evaluated using RAGAS, DeepEval, and LLM-as-Judge on HotpotQA. Dual-backend: runs locally via Ollama or via API. Dockerized.
